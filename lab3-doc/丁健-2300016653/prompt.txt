# AI提示词记录

## 项目开发思路
1. 基于已有后端API开发完整ToDoList应用
2. 实现前后端分离架构
3. 使用Flask作为后端，HTML+JS作为前端
4. 通过fetch API实现前后端交互

## 关键提示词
- "基于已有的后端代码开发完整的ToDoList应用"
- "创建项目目录结构"
- "实现前端页面和交互逻辑"
- "设计响应式样式"
- "编写完整的项目文档"

## 开发过程
1. 首先迁移已有后端代码到新项目
2. 创建基础HTML结构
3. 实现JavaScript交互逻辑
4. 设计CSS样式
5. 编写项目说明文档

## 注意事项
- 保持前后端接口一致
- 确保响应式设计
- 提供完整的错误处理
- 保持代码简洁易读

---

修改app.py和相关文件：
## 更新类定义：
###支持todolist分多个group：默认初始有3个group，分别是默认、工作、入门。支持新建、重命名、删除任意group。入门分组里包括几条使用tips。添加新的todo时有下拉框选择分组，默认为“默认”
###todo任务支持重要级分类：0-3星，默认0星，用线条星形和填充星形代表
### todo可标记完成，用可交互单选框，单击交互

用户拒绝了此操作，并提供了以下反馈： 我不同意这个类定义。我希望保持一致的思路，将组名作为数据属性分开管理。比如开一个变量记录idx和组名，或者类json地在｛｝内同时包含数据和组名。现在这样使用组名做key不方便调用特定的组以及重命名。同时self.todos变量和groups中的‘默认分组’不应该同时存在，建议仍然沿用self.todos（其他group分开存储），使之成为各类方法的默认值，并是一个无法删除的默认组