首先，你需要在该项目的`/project`文件夹下建立文件`app.py`。

似乎5000端口被AirPlay服务占用了。有什么方法关闭它吗？

现在已经将AirPlay相关功能禁用。在app.py的基础上，你需要添加一个类`ToDoList`，其中包含以下内容：

1. 文本内容；

2. 标记完成与否的变量；

3. 分类；

4. 优先级，分为低、中与高三类。

现在api确实有输出，但所有的非ASCII字符都被转义了！如何避免这一行为？

接下来你需要在project文件夹下建立一个前端APP。该APP由3个组件组成：`index.html`, `script.js`与`style.css`. 无需使用nodejs。

现在该app能显示界面，但无法与后端通信。报错信息显示，问题与CORS policy相关。如何修复？

现在前端能正常工作了。修改App，使其能对代办事项按优先级和分类展示