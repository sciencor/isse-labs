首先，你需要在该项目的`/project`文件夹下建立文件`app.py`。

似乎5000端口被AirPlay服务占用了。有什么方法关闭它吗？

现在已经将AirPlay相关功能禁用。在app.py的基础上，你需要添加一个类`ToDoList`，其中包含以下内容：

1. 文本内容；

2. 标记完成与否的变量；

3. 分类；

4. 优先级，分为低、中与高三类。

现在api确实有输出，但所有的非ASCII字符都被转义了！如何避免这一行为？