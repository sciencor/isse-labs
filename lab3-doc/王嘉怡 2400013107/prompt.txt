帮我做一个TodoList系统完成以下功能：
1. 输入待办事项
2. 所有事项展示在列表中，未完成在前，已完成在后
3. 未完成实现有三种可选的排列方式：按创建日期前后、按结束日期前后、按重要等级
4. 用户可以置顶事项，也就是将该事项固定到序列最上方，同时加上旗标
5. 可删除任务、编辑任务、把任务标记为完成
6. 可标记重要事项，显示为星
7. 可将事项分类

这是第一步：后端接口设计
设计Flask 后端，包含以下路由：
GET /tasks：获取全部任务；
POST /tasks：新增任务；
PUT /tasks/<id>：修改任务状态（完成/未完成）；
DELETE /tasks/<id>：删除任务；
EDIT /tasks/<id>：编辑任务；
GET /tasks?category=xxx：分类
GET /tasks?flag=true：标记置顶（旗标）
GET /tasks?star=true：标记星
每个任务对象字段如下：
{ "id": 1, "title": "写实验报告", "category": "学习", "flag": true, "star": true, "completed": false }
暂不使用数据库，用内存列表（或JSON 文件）模拟存储。
后端返回统一格式：
{ "status": "success", "data": [...], "message": "新增成功" }
编写app.py，测试接口在Postman或浏览器中能正常返回。

现在，请给我编写基于Postman的app.py的调试文件，包括测试样例和测试代码。
测试样例格式是这样的：{ "id": 1, "title": "写实验报告", "category": "学习", "flag": true, "star": true, "completed": false }。
我对Postman比较生疏，请告诉我具体操作步骤。

现在来进行第二步：前端页面结构设计
设计一个主页面index.html，包含以下部分：
可以切换黑夜/白天模式；
输入框（输入待办事项标题）；
下拉菜单（选择分类与优先级）；
“添加任务”按钮；
任务排序下拉框（包括按字典名称序、按创建日期前后、按结束日期前后、星级是否在前）；
任务展示区（列表形式，每项包含任务名、类别、优先级、状态、操
作按钮）。
页面使用简洁布局：
顶部：输入表单；
中间：任务列表（包含任务排序三种方案）
底部：筛选功能区（按分类/优先级）。
样式说明：
未完成任务灰色；
已完成任务加删除线；
星级任务加星星标识；
红旗置顶任务排在任务最上面，加红旗标识。

现在请新建一个css文件，用来定义网页的样式和颜色。
我希望是整体样式对比度比较明显的莫兰迪色系。字体可以再大再粗些。
同时请更改“年月日”的字体与其他一致。

你似乎没有集成上css，我现在显示出来的是很丑陋的一个普通页面。
帮我修改一下。

夜间模式的配色再改改。我希望字体颜色夜间可以是温和而清楚的正黄色。
另外日期那里的字体还是不对的。只有那个是宋体，希望能改成和其他的一样。
第三，这个黑夜模式上面有一条白，把它也变成黑色，不要出这种bug。

第一，黑夜模式的下拉框还是白色的，希望把它变成黑色。
第二，下拉框的样子可以做成圆角。
第三，这个黑夜模式上面有一条白，把它也变成黑色，刚才还是没有改成，我是chrome浏览器。
第四，灰色的功能指示框变成灰蓝色试试？黑夜白天都改变。

下拉框的颜色要和背景有所区分，比如黑夜模式改成深灰色，白天模式改成淡灰色。
现在“取消完成”“添加”这些功能按钮还是没有成功渲染，就按最开始的版本渲染就可以了。